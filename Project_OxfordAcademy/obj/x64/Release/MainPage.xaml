<Page
    x:Class="Project_OxfordAcademy.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Project_OxfordAcademy"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <StackPanel Orientation="Vertical" Margin="50,50, 10, 50">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Stretch">
            <Border Width="40" Height="40" CornerRadius="10" BorderThickness="1" BorderBrush="LightSkyBlue" Padding="5">

                <Border>

                    <Border.Background>
                        <ImageBrush ImageSource="Assets/employee_office.png" />
                    </Border.Background>

                </Border>

            </Border>
            <TextBlock Text="Teachers of Oxford Academy" Margin="50" FontStyle="Oblique" FontSize="50" Foreground="DarkCyan" FontStretch="Condensed" FontWeight="Bold" Height="66" Width="685" />
        </StackPanel>
        <ScrollViewer Height="580">
            <ListView x:ConnectionId='2' x:Name="listTeacher"   Margin="0,10,0,0"                                                 >
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal"  ></ItemsWrapGrid>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="1" CornerRadius="5" BorderBrush="Blue" Background="LightSkyBlue" Margin="10">
                            <StackPanel Orientation="Vertical" Margin="10">
                                <Border BorderBrush="Azure" 
                                Width="300" Height="300"
                                BorderThickness="3"
                                CornerRadius="5"                                   
                               Padding="2">
                                    <Border BorderThickness="5"
                                    CornerRadius="5"                                   
                                    Padding="5"
                
                           >
                                        <Border.Background>
                                            <ImageBrush ImageSource="{Binding Image}" />
                                        </Border.Background>
                                    </Border>
                                </Border>


                                <StackPanel Orientation="Horizontal" VerticalAlignment="Stretch" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" Margin="10, 10, 0, 10" FontWeight="Medium" />
                                    <TextBlock Text="," Margin="1, 10, 3, 10" />
                                    <TextBlock Text="{Binding Post}" HorizontalAlignment="Center" Margin="0, 10, 10, 10" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
        </ScrollViewer>
        <TextBlock Text="Developed by MAKSUDA YESMIN" FontStyle="Italic" TextAlignment="Right" Foreground="DarkCyan" Margin="10" />
        <TextBlock Text="Trainee id:1267819" TextAlignment="Right" FontStyle="Italic" Foreground="DarkCyan" Margin="10" />
    </StackPanel>

</Page>

